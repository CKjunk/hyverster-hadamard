-外部高速晶振：25MHz
 各总线运行时钟：
-系统时钟 = SYCCLK = AHB1 = 180MHz
-APB2 = 90MHz 
-APB1 = 45MHz
 
 定时器挂载在AHB1上 频率为180m
 定时器预分频 179 一个周期为1us 谱图周期20ms 需要两万个点
 
 
 1.检查ADC初始的几个值错误是否是adc的问题还是matlab接收的问题。
 为了使ADC采样更加精准，设置先让adc采样五次
串口调试助手 
 * ADC采样的第一个数是错误值，应丢弃，从第二个数开始才是正确的数据，
 * 因为有两个无效的adc的采集周期，所以有两个采集点需要抛弃，加上需要的1023个采集点，一共需要的采集点为1026个点
 
 matlab数据
 *最前面一个点是零，从第二个数开始是有效数据，所以算是1027个点
 
 2.可以在matlab输出图像之后，发送一个字符回来，重新开始采集信号